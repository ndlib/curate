<%= f.simple_fields_for :degree do |df| %>
  <%= df.input :level, label: 'Degree Level', input_html: { value: df.object.level.first } %>
  <%= df.input :discipline, label: 'Degree Discipline', input_html: { value: df.object.discipline.first } %>
  <%= df.input :name, label: 'Degree Name', input_html: { value: df.object.name.first } %>
<% end %>
